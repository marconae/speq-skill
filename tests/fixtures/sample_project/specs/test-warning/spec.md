# Feature: Test Warning Spec

This spec has warnings but no errors.

## Background

The system is ready.

## Scenarios

### Scenario: Too many AND steps

* *GIVEN* state one
* *AND* state two
* *AND* state three
* *AND* state four
* *AND* state five
* *WHEN* an action occurs
* *THEN* the system SHALL respond
